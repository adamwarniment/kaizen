# Production Environment Variables

# GitHub Container Registry Configuration
# Your GitHub username or organization name (lowercase)
GITHUB_REPOSITORY_OWNER=your-username
# Your repository name (lowercase)
GITHUB_REPOSITORY_NAME=goalgetter

# Application Ports
WEB_PORT=8080
API_PORT=3001
DB_PORT=5432

# Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=kaizen

# Backend Configuration (Optional overrides)
# DATABASE_URL is constructed automatically in docker-compose.prod.yml
# but can be overridden here if needed.
